{
  "name": "@mourner/claudefiles",
  "version": "0.1.0",
  "type": "module",
  "description": "Claude Code efficiency tools: a PreToolUse guard hook and a cost/usage status line.",
  "license": "MIT",
  "scripts": {
    "pretest": "eslint hooks/guard.mjs hooks/test/*.test.js statusline/test/*.test.js",
    "test": "node --test"
  },
  "devDependencies": {
    "eslint": "^10.4.1",
    "eslint-config-mourner": "^5.0.1"
  }
}
